DTO PROJECT - MASTER STATUS AND ERROR CODE REFERENCE
Generated: April 16, 2026
Format: CODE-XXX | Title | Full Description
Scope: 0XX = General (DTOController + DTOManualTesting) | 1XX = DTOController.cpp only | 2XX = DTOManualTesting.cpp only | 3XX = computations.py only
================================================================================================================================================


GENERAL CODES (0XX) - Emitted by both DTOController.cpp and DTOManualTesting.cpp
------------------------------------------------------------------------------------------------------------------------------------------------

STATUS-001 | Session Started | main() entered the primary while-loop after all file and GPIO initialization completed; the first loop iteration is about to begin.

STATUS-002 | Session Ended | The ESC key was detected and the main while-loop exited cleanly; the cleanup and shutdown sequence is beginning.

STATUS-003 | Shutdown Successful | All GPIO pins have been deactivated, log files have been finalized, and the program is returning exit code 0.

STATUS-005 | Log Directory Created Successfully | The mkdir command targeting logDirectoryPath returned exit code 0, confirming the Logs/ directory was created and is ready for file I/O.

STATUS-008 | Both Log Files Opened and CSV Headers Written | Both Keybind_Log.csv and Activity_Log.csv were opened successfully with ios::trunc and their CSV header rows were written before the session began.

STATUS-009 | Startup Complete: All Files Ready for Logging | All pre-session validation steps passed; both log files are open, verified, and ready to receive entries for the current session.

STATUS-011 | Key Registered | A key input was matched to a known translation or rotation action in processMovementAction() and a corresponding row was successfully written to Keybind_Log.csv.

STATUS-012 | Startup Sequence Initiated | The user pressed '1' in Menu Mode; programMode was set to 1 and the three-rack GPIO validation loop is beginning.

STATUS-013 | Startup Sequence Complete: All 12 GPIO Successfully Activated | All three rack connector tests completed and failedGPIOPins is empty; all 12 pins were activated without error.

STATUS-014 | Rack Connector Test Started | The outer rack loop entered a new iteration; the rack number is included in the description and both Phase 1 and Phase 2 are about to execute for this connector.

STATUS-015 | Rack Connector Test Passed | All four GPIO pins in this rack connector completed both Phase 1 (four 1-second pulses) and Phase 2 (four double 0.5-second pulses) without any activation failure.

STATUS-016 | GPIO Pin Activated (ON) | A specific GPIO pin was commanded ON during the startup sequence; the pin number and pulse type (standard or PULSE) are included in the description.

STATUS-017 | GPIO Pin Deactivated (OFF) | A specific GPIO pin was commanded OFF during the startup sequence after its pulse duration elapsed; the pin number and pulse type are included in the description.

STATUS-018 | Startup Sequence Complete: Partial Activation Detected | All three rack tests completed but failedGPIOPins is non-empty; the count of successful and failed pins is included in the description.

STATUS-019 | UI Menu Refreshed | displayMenu() was called; a STATUS-019 entry is written before printing the mode-appropriate menu to stdout so UI redraws are traceable in the activity log.

STATUS-020 | Operational Mode Activated | The user pressed '2' in Menu Mode; programMode was set to 2 and the polling loop is now ready to accept real-time movement and rotation keybind inputs.

ERROR-000 | Startup Failure: Log Files Could Not Be Opened | Either keybindLogFile or activityLogFile was not is_open() after the ios::trunc open attempt; the program logs this entry and returns exit code 1 immediately.

ERROR-002 | Keybind Log Write Failure | An attempt to open Keybind_Log.csv in append mode inside logKeyData() failed; the key event was not written to disk. The logActivity call still succeeds for console output.

ERROR-003 | Log Directory Creation Failed | The system() mkdir command returned a non-zero exit code; the Logs/ directory may not exist and subsequent log file opens may fail.

ERROR-004 | Incorrect Keybind | A character was received in Operational Mode that does not match any key in the translation or rotation maps inside processMovementAction(); the unrecognized key is logged and no GPIO action is taken.

ERROR-005 | Startup Sequence Aborted by User via ESC | An ESC input was detected during the startup sequence before all three rack connector tests completed; force_exit was set to true and remaining rack iterations were skipped.

ERROR-006 | Rack Connector Test Failed | A specific GPIO pin within a rack connector test failed to activate; the rack number and pin number are included in the description and the pin is added to failedGPIOPins.

ERROR-007 | GPIO Activation Failed: Summary Entry | After all rack tests, one or more pins remain in failedGPIOPins; a separate ERROR-007 entry is written per failed pin before the STATUS-018 partial activation summary.


DTCONTROLLER.CPP SPECIFIC CODES (1XX)
------------------------------------------------------------------------------------------------------------------------------------------------

STATUS-100 | GPIO Chip Opened at Primary Path | gpiod_chip_open("/dev/gpiochip4") returned a non-null handle; the global gpioChip variable is valid and all subsequent pin operations will use gpiochip4.

STATUS-101 | GPIO Chip Opened via Fallback Path | The primary /dev/gpiochip4 path failed; gpiod_chip_open("/dev/gpiochip0") succeeded and returned a valid gpioChip handle for all subsequent pin operations.

STATUS-102 | Terminal Configured for Raw Non-Blocking Input | On the first call to checkKeyboardInput(), tcsetattr() disabled ICANON and ECHO on stdin so individual keystrokes are available without waiting for Enter.

STATUS-103 | stdin O_NONBLOCK Flag Set | fcntl() F_SETFL applied the O_NONBLOCK flag to stdin; getchar() will now return immediately with EOF instead of blocking when no input is available.

STATUS-104 | Translation Keybind Dispatched | A translation key (W/S/A/D/E/Q) was matched in processMovementAction(); logKeyData() was called and both associated GPIO thruster pins were set to ON via setGPIOPin().

STATUS-105 | Rotation Keybind Dispatched | A rotation key (I/K/U/O/J/L) was matched in processMovementAction(); logKeyData() was called and both associated GPIO thruster pins were set to ON via setGPIOPin().

STATUS-106 | All 12 Thrusters Deactivated on Key Release | noInputCount reached the RELEASE_CYCLES threshold in the no-input else branch; all 12 GPIO pins were set to 0, currentKeyPressed was cleared, and noInputCount was reset. Also emitted by DTOManualTesting.cpp for the same condition.

STATUS-107 | All 12 Thrusters Deactivated on Program Exit | The ESC exit path set all 12 GPIO pins to 0 via setGPIOPin() before closing the gpiod chip handle and returning; guarantees all thrusters are off on shutdown. Also emitted by DTOManualTesting.cpp for the same condition.

STATUS-108 | GPIO Line Request Held for Startup Pulse | requestGPIOPin() returned a non-null line request handle for a pin during the startup sequence; the handle will be passed to releaseGPIORequest() after the sleep duration.

STATUS-109 | Held GPIO Line Request Released After Pulse Duration | releaseGPIORequest() was called with a valid line request handle obtained from requestGPIOPin(); the GPIO pin returns to its default state and the line is freed.

STATUS-110 | Startup Sequence Phase 1 Entered for Rack | The inner Phase 1 for-loop began for this rack connector; four individual 1-second ON/OFF pulses are about to execute on GPIO pins 0-3, 4-7, or 8-11.

STATUS-111 | Startup Sequence Phase 2 Entered for Rack | The inner Phase 2 for-loop began for this rack connector; four double 0.5-second pulse pairs are about to execute on this rack's four GPIO pins.

STATUS-112 | Duplicate GPIO Failure Entry Suppressed | During Phase 2 of the startup sequence, std::find confirmed the current gpioPin is already in failedGPIOPins; the duplicate push_back was skipped to keep failure records accurate.

STATUS-113 | ESC Force Exit Triggered During Startup | An ESC byte was detected via checkKeyboardInput() at a Phase 1 or Phase 2 boundary check; force_exit was set to true, breaking the inner loops and skipping all remaining rack iterations.

STATUS-114 | Returned to Menu from Operational Mode | The user pressed '0' while in Operational Mode; if currentKeyPressed was non-empty all 12 GPIO pins were first deactivated, then programMode was reset to 0 and displayMenu() was called.

ERROR-100 | GPIO Chip Initialization Failed | Both gpiod_chip_open("/dev/gpiochip4") and the fallback gpiod_chip_open("/dev/gpiochip0") returned null; gpioChip remains null and all subsequent setGPIOPin() and requestGPIOPin() calls will return false or nullptr.

ERROR-101 | GPIO Primary Path Failed: Fallback Initiated | gpiod_chip_open("/dev/gpiochip4") returned null; this error is logged before attempting the /dev/gpiochip0 fallback so the primary failure is traceable even if the fallback succeeds.

ERROR-102 | GPIO Not Initialized: gpioChip is Null | setGPIOPin() or requestGPIOPin() was called but gpioChip is null because initGPIO() failed or was never called; the function returns false or nullptr immediately without attempting any libgpiod calls.

ERROR-103 | GPIO Line Request Failed | gpiod_chip_request_lines() returned null for a specific pin offset inside setGPIOPin(); the pin could not be set to the desired value and the function returns false after freeing all allocated structs.

ERROR-104 | GPIO Pin Request Failed During Startup Sequence | requestGPIOPin() returned null for a pin during Phase 1 or Phase 2 of the startup sequence; the pin number was added to failedGPIOPins for the final summary report.

ERROR-105 | Unrecognized Menu Input Received | A character was received in Menu Mode that does not match '1', '2', or ESC (27); no mode change is performed and the character is discarded silently.


DTOMANUALTESTING.CPP SPECIFIC CODES (2XX)
------------------------------------------------------------------------------------------------------------------------------------------------

STATUS-200 | Translation Action Simulated | A translation key (W/S/A/D/E/Q) was matched in processMovementAction(); logKeyData() was called and the corresponding [SIM] GPIO pair console output was written to stdout in place of real GPIO activation.

STATUS-201 | Rotation Action Simulated | A rotation key (I/K/U/O/J/L) was matched in processMovementAction(); logKeyData() was called and the corresponding [SIM] GPIO pair console output was written to stdout in place of real GPIO activation.

STATUS-202 | All Simulated GPIO Deactivated | [SIM] All GPIO OFF was written to stdout when the key release threshold was reached or when the user pressed '0' to exit Operational Mode; mirrors the DTOController hardware deactivation behavior.

STATUS-203 | Startup Sequence Simulated Successfully | All three rack connector tests completed the simulated phase loops without ESC interruption; the hardcoded "All 12/12 GPIO Successfully Activated" message was printed and STATUS-013 was logged.

STATUS-204 | ESC Detected via GetAsyncKeyState During Startup | GetAsyncKeyState(VK_ESCAPE) returned a value with the high bit set inside a Phase 1 or Phase 2 boundary check; force_exit was set to true and remaining rack iterations were skipped.

ERROR-200 | Windows mkdir Failed to Create Log Directory | The system("mkdir ./Logs/ 2>nul") command returned a non-zero exit code; the Logs/ directory may not exist and subsequent log file opens may fail.


COMPUTATIONS.PY SPECIFIC CODES (3XX)
------------------------------------------------------------------------------------------------------------------------------------------------

STATUS-300 | Script Initialization Started | The first log_event() call executed successfully, confirming the logging function is operational; the script is entering its configuration and variable setup phase.

STATUS-301 | Configuration Variables Loaded | All configuration constants and lookup structures have been defined: step_increment, rot_increment (in radians), base_translation dict, rotational_moves dict, and dof_colors dict are all ready for use.

STATUS-302 | File Path Validated | os.path.exists() returned True for the configured file_path value, confirming the Keybind CSV is accessible before attempting pd.read_csv().

STATUS-303 | CSV Data Loaded | pd.read_csv() parsed the file successfully; both the Time(s) and Direction columns were confirmed present in df.columns, and the Time(s) column was converted to float via parse_time_value(). Total row count is logged.

STATUS-304 | 3D Chunk Phase Started | The full trajectory segment list was built by iterating all Type-T movement rows; total_chunks was calculated from math.ceil(max_time / CHUNK_DURATION) and the per-chunk for-loop is about to begin.

STATUS-305 | Processing Chunk N | The chunk for-loop entered a new iteration; t_start and t_end were computed, and the segment clipping loop is filtering and trimming all segments that overlap this time window.

STATUS-306 | Chunk Skipped | After the segment clipping loop, chunk_segs is empty because no segments overlap this chunk's [t_start, t_end] window; the iteration continues to the next chunk via continue.

STATUS-307 | 3D Trajectory Chunk Saved to PNG | plt.savefig() completed writing the chunk figure to Images/3D_Chunk_NN.png at 150 dpi; plt.close(fig) was called immediately after to release the figure from memory.

STATUS-308 | 3D Chunk Figure Initialized | plt.figure() and add_subplot(111, projection='3d') were called for this chunk; the Axes3D object is ready for pane color setup and trajectory plotting.

STATUS-309 | Full Telemetry Processing Phase Started | The telemetry processing block was entered; Type-T rows were deduplicated on Time(s) and sorted ascending so the integration loop will process events in correct chronological order.

STATUS-310 | Global Trajectory Integration Complete | The full_directions for-loop finished processing all deduplicated movement rows; positions_arr and attitudes_arr (Roll/Pitch/Yaw in degrees) have been assembled covering the entire session timeline.

STATUS-311 | Velocity and Acceleration Arrays Computed | numpy.gradient() was applied to pos_x, pos_y, and pos_z using full_times as the non-uniform spacing argument, producing vel_x/y/z; a second gradient pass on the velocity arrays produced acc_x/y/z.

STATUS-312 | 2D Telemetry Plot Phase Started | All prerequisite arrays (positions, attitudes, velocities, accelerations) are fully built; the four sequential 2D plot generation and save calls are beginning.

STATUS-313 | Absolute Position Plot Saved | The Absolute Position figure with pos_x, pos_y, pos_z plotted against full_times was completed; plt.savefig() wrote Images/Position.png at 150 dpi and the figure was closed.

STATUS-314 | Attitude Plot Saved | The Attitude figure with roll_arr, pitch_arr, yaw_arr in degrees plotted against full_times was completed; plt.savefig() wrote Images/Attitude.png at 150 dpi and the figure was closed.

STATUS-315 | Linear Velocity Plot Saved | The Linear Velocity figure with vel_x, vel_y, vel_z plotted against full_times was completed; plt.savefig() wrote Images/Velocity.png at 150 dpi and the figure was closed.

STATUS-316 | Linear Acceleration Plot Saved | The Linear Acceleration figure with acc_x, acc_y, acc_z plotted against full_times was completed; plt.savefig() wrote Images/Acceleration.png at 150 dpi and the figure was closed.

STATUS-317 | Script Execution Completed Successfully | All chunk PNG files and all four 2D telemetry PNG files have been written to the Images directory; this is the final log_event() call before the script exits.

STATUS-318 | Full Trajectory Segment List Built | The segment construction for-loop over all Type-T movement rows completed; total segment count and source row count are logged before the per-chunk plotting loop begins.

STATUS-319 | Chunk Segments Prepared | The segment clipping loop for this chunk finished; cross-boundary segments were trimmed so clip_fp and clip_tp fall exactly on the chunk boundaries, and the total number of segments in this window is logged.

STATUS-320 | Tick Label Placed on 3D Plot | interp_pos_at() returned a valid position for this tick time; ax.text() was called with the interpolated coordinates offset by text_offset to place the time string label on the 3D figure.

STATUS-321 | All Tick Labels Placed for Chunk | The tick label while-loop completed for this chunk without raising an exception; all time labels from the first valid tick up to min(t_end, max_time) have been written to the figure.

STATUS-322 | 3D Axis Limits Applied | The bounding box of all_chunk_pts was computed via pts_arr; max_range, mid_x, mid_y, mid_z, and buf were derived, and set_xlim(), set_ylim(), set_zlim() were called to produce a uniform cubic axis space.

STATUS-323 | max_range Defaulted to 1.0 | The computed max_range was 0.0 because all trajectory points in this chunk are identical (vehicle did not move); it was overridden to 1.0 to prevent zero-extent axis limits from collapsing the 3D plot.

STATUS-324 | Full Telemetry Rotation Move Processed | A direction code was matched in rotational_moves during the full telemetry integration loop; get_rotation_matrix() produced a new rotation matrix that was pre-multiplied into global_rot_matrix via np.dot().

STATUS-325 | Full Telemetry Translation Move Processed | A direction code was matched in base_translation during the full telemetry integration loop; the body-frame vector was rotated to world frame via np.dot(global_rot_matrix, base_move_vec) and added to global_pos.

STATUS-326 | Gimbal Lock Detected | rotation_matrix_to_euler() computed sy < 1e-6, indicating a near-singular rotation matrix; the alternate fallback atan2 expressions were used for roll and pitch and yaw was forced to 0 degrees.

STATUS-327 | 3D Plot Pane Colors Set to White | ax.xaxis.pane.set_facecolor('white'), ax.yaxis.pane.set_facecolor('white'), and ax.zaxis.pane.set_facecolor('white') were called to produce a clean white background on all three axis panes.

STATUS-328 | 3D Plot View Angle Initialized | ax.view_init(elev=20, azim=-50) was called to set the default camera perspective for this chunk's 3D trajectory figure before saving to PNG.

ERROR-300 | File Access Failed | os.path.exists() returned False for the configured file_path value; a FileNotFoundError is raised after logging and the outermost except FileNotFoundError handler silently passes since the error was already recorded.

ERROR-301 | Data Parsing Failed: Column Missing | pd.read_csv() succeeded but the resulting DataFrame is missing the Time(s) or Direction column; a KeyError is raised after logging and the outermost except KeyError handler silently passes.

ERROR-302 | 3D Chunk Processing Error | An exception was raised inside the chunk generation try block during segment building, clipping, or matplotlib figure operations; the error message is included in the description and the exception is re-raised to the outer handler.

ERROR-303 | Telemetry Integration Error | An exception was raised during the full-dataset integration for-loop or during the numpy.gradient derivative calculation; the error message is included and the exception is re-raised to the outer handler.

ERROR-304 | Unexpected General Error | An exception that is neither FileNotFoundError nor KeyError reached the outermost except block; the error message is logged and script execution ends without further processing.


================================================================================================================================================
END OF CODE REFERENCE
Total Codes: 47 STATUS codes | 22 ERROR codes | 69 total
Breakdown: 20 shared 0XX | 15 DTOController 1XX | 10 DTOManualTesting 2XX | 29 computations.py 3XX | 7 shared errors | 8 controller errors | 1 manual testing error | 6 computations errors
================================================================================================================================================

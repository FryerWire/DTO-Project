
Changelogs

From the previous GitHub commit, do the following:
1. Write in markdown. 
2. Do NOT remove or modify any current existing content. Only write to DTO-Project\Logs\changelogs.md with the newest and latest version update. 
3. Use all of the documents in the directory to gather informational changes. 
4. Use the 'Example' below as an idea of how I want it to look like. 
5. Keep one line of whitespace for the first line and the very last line. 
6. Write a description of all of the changes that have happened since the last commit.
7. Update this as the newest verion. vx.x.x+1

Example:
# Changelogs

## Version X.X.X
Overall summary of all the additions and changes.  

### New Features
- Added **computation.py** file in **DTO-Project/Computation/** folder. 
- Created **function name** in **computation.py** that has the following input and output, and a description of what it does. 
- Important variables (No minor variables like x = 3 + user_inut) and what they do. 

### Changes
- Changed **function name** in **computation.py** from having x inputs to y inputs, and w outputs to z ouputs. Modified what the function does...
- Changed **name** to **name** in **computation.py**

### Removed
- Removed **function name** in **computation.py** for simplicity

### Bug Fixes
- Fixed output value error in **function name** in **computation.py** to have a correct unit now. 
